import "./styles.scss";
import {
  addNoNotesAddNoteListener,
  addScrollUpListener,
  addSearchNotesListener,
} from "./utils/listeners";

addNoNotesAddNoteListener();
addSearchNotesListener();
addScrollUpListener();
